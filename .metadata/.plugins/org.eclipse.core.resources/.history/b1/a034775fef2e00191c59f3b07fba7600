package ejm118_music1;

import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

public class Song {
	private String songID;
	private String title;
	private int length;
	private String filePath;
	private String releaseDate;
	private String recordDate;
	private DbUtilities db;
	private List<Artist> songArtist = new ArrayList<Artist>();
	
	
	public Song (String title, String releaseDate, String recordDate)
	{
		songID = UUID.randomUUID().toString();
		this.title = title;
		this.releaseDate = releaseDate;
		this.recordDate = recordDate;
		
		DbUtilities db = new DbUtilities();
		String sql = "INSERT INTO song (title, release_date, record_date, song_id, length) VALUES ('" + title + "' , '" + releaseDate + "' , '" + recordDate + "' , '" + songID + "' , '" + 3 + ";)";
		System.out.println(sql);
		db.executeQuery(sql);
	}
	
	public Song(String songID)
	{
		this.songID = songID;
		db = new DbUtilities();
		
	}
	
	public void deleteSong(String songID)
	{
		
	}
	
	public void addArtist(String artistID)
	{
		
	}
	
	public void deleteArtist(Artist artist)
	{
		
	}
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	public String getTitle() {
		return title;
	}
	public void setTitle(String title) {
		
		DbUtilities db = new DbUtilities();
		String sql = "UPDATE Song SET title = '" + title + "'WHERE songID = " + this.songID + ";";
		this.title = title;	
		db.executeQuery(sql);
		
	}
	public int getLength() {
		return length;
	}
	public void setLength(int length) {
		DbUtilities db = new DbUtilities();
		String sql = "UPDATE Song SET length = '" + length + "'WHERE songID = " + this.songID + ";";
		this.length = length;	
		db.executeQuery(sql);
	}
	public String getFilePath() {
		return filePath;
	}
	public void setFilePath(String filePath) {
		DbUtilities db = new DbUtilities();
		String sql = "UPDATE Song SET filePath = '" + filePath + "'WHERE songID = " + this.songID + ";";
		this.filePath = filePath;	
		db.executeQuery(sql);
	}
	
	public String getReleaseDate() {
		return releaseDate;
	}
	public void setReleaseDate(String releaseDate) {
		DbUtilities db = new DbUtilities();
		String sql = "UPDATE Song SET releaseDate = '" + releaseDate + "'WHERE songID = " + this.songID + ";";
		this.releaseDate = releaseDate;	
		db.executeQuery(sql);
	}
	public String getRecordDate() {
		return recordDate;
	}
	public void setRecordDate(String recordDate) {
		DbUtilities db = new DbUtilities();
		String sql = "UPDATE Song SET recordDate = '" + recordDate + "'WHERE songID = " + this.songID + ";";
		this.recordDate = recordDate;	
		db.executeQuery(sql);	}
	public String getSongID() {
		return songID;
	}
	
	
	
}
